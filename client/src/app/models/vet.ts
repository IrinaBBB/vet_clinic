export interface Vet {
    id: number
    name: string
    dateOfBirth: string // DateOnly in C# can be represented as a string in TypeScript
    dateOfGraduation: string // DateOnly in C# can be represented as a string in TypeScript
}